import React from 'react';
import { Container } from 'react-bootstrap';

const Login = () => {
    return (
        <div>
            <Container className="common-container">Login</Container>
        </div>
    );
};

export default Login;
